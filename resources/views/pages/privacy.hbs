<!doctype html>
<html lang="en">
<head>
  {{> ../common/head }}
</head>
<body>
  {{> ../common/header}}
  {{> ../partials/page-intro}}
  <section class="container">
    <div class="max-w-screen-md mx-auto my-10 lg:my-16 leading-6 text-base">
      {{#each paragraph}}
        <div class="mb-12">
          <h3 class="text-black mb-5">{{title}}</h3>
          <p>{{text}}</p>
        </div>
      {{/each}}
    </div>
  </section>
  {{> ../common/footer}}
</body>
</html>
